# FastApi Crud Orm Connector

Simple methods for fast prototyping crud operations connecting fastapi with datasets, such as sqlalchemy and pandas

Installation:
```bash
pip install fastapi_crud_orm_connector
```
